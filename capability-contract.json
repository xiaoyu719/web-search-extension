{
  "schemaVersion": 1,
  "minimum": {
    "sillytavern": "1.18.0",
    "tavernHelper": null
  },
  "requirements": [
    {
      "id": "host-context",
      "owner": "sillytavern",
      "symbol": "SillyTavern.getContext",
      "required": true,
      "fallback": null
    },
    {
      "id": "set-extension-prompt",
      "owner": "sillytavern",
      "symbol": "SillyTavern.getContext().setExtensionPrompt",
      "required": true,
      "fallback": null
    },
    {
      "id": "slash-command-parser",
      "owner": "sillytavern",
      "symbol": "SillyTavern.getContext().SlashCommandParser",
      "required": false,
      "fallback": "斜杠命令 /websearch 不注册，仍可用关键词触发和设置页试搜"
    },
    {
      "id": "world-info-write",
      "owner": "sillytavern",
      "symbol": "SillyTavern.getContext().loadWorldInfo",
      "required": false,
      "fallback": "宿主没有世界书读写接口时跳过写入，搜索和提示词注入仍可用"
    },
    {
      "id": "world-info-save",
      "owner": "sillytavern",
      "symbol": "SillyTavern.getContext().saveWorldInfo",
      "required": false,
      "fallback": "宿主没有世界书保存接口时跳过写入，搜索和提示词注入仍可用"
    },
    {
      "id": "tauritavern-global",
      "owner": "provider",
      "symbol": "window.__TAURITAVERN__",
      "required": false,
      "fallback": "按浏览器 SillyTavern 处理；设置页显示 SillyTavern"
    }
  ]
}
